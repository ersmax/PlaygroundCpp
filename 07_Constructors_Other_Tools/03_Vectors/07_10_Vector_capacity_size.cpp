// What is the difference between the size and the capacity of a vector?
// Size = n elements of a vector
// Capacity = n elements for which the vector currently has memory allocated

#include <iostream>

int main( )
{
	std::cout << '\n';
	return 0;
}
