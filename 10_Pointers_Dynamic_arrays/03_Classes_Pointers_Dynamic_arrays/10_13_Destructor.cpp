//If a class is named MyClass and it has a constructor, what is the constructor
//named ? If MyClass has a destructor what is the destructor named ?

// MyClass and ~MyClass

int main( )
{
	return 0;
}